
jwt:
  header: Authorization
  secret: my-very-secret-key
  pubkey: pppubkey.pem
  azurejwk: https://login.microsoftonline.com/f1651775-d631-4149-936d-287f391c646b/discovery/v2.0/keys
  usijwk: https://logint.maerskline.com/jwt-rest/jwks.json
logging:
  level:
    org.springframework.security: DEBUG
server:
  port: 8080
spring:
  resources:
    chain:
      enabled: true
management:
  security:
    enabled: true # set to false to disable 'default' Spring Boot security
#endpoints:
#  cors:
#    allowed-origins: '*'
